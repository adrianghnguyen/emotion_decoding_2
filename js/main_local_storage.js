function process_local_storage(){
    const local_keys = Object.keys(localStorage);
    
    // Do stuff for each saved session
    local_keys.forEach(individual_session => {
        //function1
        //function2
    });
}

